
--- server 0:
   name = 127.0.0.1
   listen = 4040
   root = ./www/
   index = index.html
   body = 90000000
   listing = on
   method =    GET    POST 
   error pages:
   error 404 = ./www/error/error.html
   - location 0:
       dir = /upload
       root = ./www/
       index = upload.html
       listing = on
       redir = 
       methods = GET POST 
   - location 1:
       dir = /login
       root = ./www/
       index = login.html
       listing = on
       redir = 
       methods = GET 
--- server 1:
   name = 0.0.0.0
   listen = 4041
   root = ./www/
   index = index2.html
   body = 900000
   listing = on
   method =    POST    GET    DELETE 
   error pages:
   error 404 = error404.html
   - location 0:
       dir = /redir
       root = ./www/
       index = index2.html
       listing = off
       redir = https://profile.intra.42.fr
       methods = GET 
--- server 2:
   name = 0.0.0.0
   listen = 4042
   root = ./www/
   index = index.html
   body = 10
   listing = on
   method =    POST    GET 
   error pages:
----- request -----


----- request -----


client closed connection
